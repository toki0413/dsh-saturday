{
  "name": "saturday",
  "version": "0.3.3",
  "private": true,
  "description": "Saturday — 材料计算的插件运行时（Everything is a plugin，宿主为 DeepSeek Harness / Cordis v4）",
  "type": "module",
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "summary": "node scripts/summary/summarize.mjs"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-timeout": "^0.1.1-rc.2"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/toki0413/dsh-saturday.git"
  },
  "bugs": {
    "url": "https://github.com/toki0413/dsh-saturday/issues"
  }
}
